Deploy the tic-tac-toe app to Vercel production.

1. Run `npm run build` locally first to catch any build errors before deploying.
2. If the build passes, run `vercel --prod` from the project root.
3. After deploy completes, confirm the live URL: https://tic-tac-toe-two-iota-73.vercel.app
4. Report the deployment status and URL.
